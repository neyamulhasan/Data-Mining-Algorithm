# Data Mining Assignment

This repository contains a script that performs the requested data mining tasks on the UCI Adult dataset located at `Dataset/adult.csv`.

Run:

```bash
python3 assignment/main.py
```

Install dependencies:

```bash
pip install -r requirements.txt
```
